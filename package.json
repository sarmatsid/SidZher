{
  "name": "hosting",
  "version": "1.0.0",
  "scripts": {
    "rust": "cargo run --release --manifest-path ./kuzhersi_back/Cargo.toml",
    "redis": "redis-server",
    "node": "node app.js",
    "start": "npm run redis & npm run rust & npm run node"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "^1.19.0",
    "express": "^4.16.4",
    "redis": "^3.1.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
